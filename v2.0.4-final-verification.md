# v2.0.4 최종 검증 보고서

## BUG-001 수정

### 원본 코드 (MainForm.cs, 라인 726-780)

```csharp
private async void LoginButton_Click(object? sender, EventArgs e)
{
    var email = _emailTextBox.Text.Trim();
    var password = _passwordTextBox.Text;
    var serverUrl = _serverUrlTextBox.Text.Trim();

    if (string.IsNullOrEmpty(email) || string.IsNullOrEmpty(password))
    {
        _loginStatusLabel.Text = "상태: 이메일과 패스워드를 입력해주세요.";
        _loginStatusLabel.ForeColor = Color.Red;
        return;
    }

    _loginButton.Enabled = false;
    _loginStatusLabel.Text = "상태: 로그인 중...";
    _loginStatusLabel.ForeColor = Color.Gray;

    try
    {
        await _authService.LoginAsync(email, password, serverUrl);

        // 로그인 정보 저장 처리
        _config = LoginConfig.Load();
        _config.SaveLoginInfo = _saveLoginInfoCheckBox.Checked;
        _config.AutoLogin = _autoLoginCheckBox.Checked;

        if (!_saveLoginInfoCheckBox.Checked)
        {
            _config.Email = "";
            _config.Password = "";
            _config.ServerUrl = "https://agent.zigso.kr";
        }

        _config.Save();

        _loginStatusLabel.Text = "상태: 로그인 성공";
        _loginStatusLabel.ForeColor = Color.Green;

        ShowMainPanel();

        // 당일 주문 목록 로드
        LoadTodayOrders();

        AutoStartListening();
    }
    catch (Exception ex)
    {
        _loginStatusLabel.Text = $"상태: {ex.Message}";
        _loginStatusLabel.ForeColor = Color.Red;
    }
    finally
    {
        _loginButton.Enabled = true;
    }
}
```

### 문제점

dev_progress.md (라인 386-400)에 명시된 BUG-001:
- **버그**: LoginButton_Click 메서드에서 "자동 로그인" 체크를 해제할 때 현재 세션의 토큰(`_config.Token = ""`)을 삭제
- **증상**: 로그인 직후 토큰이 비어서 업로드 시 "로그인이 필요합니다" 에러 발생
- **영향**: v2.0.0부터 현재까지 서버 업로드 성공 0건, 모든 주문이 로컬에만 저장

### 수정 코드

**이미 수정됨**: 현재 코드(MainForm.cs 라인 726-780)에는 `_config.Token = ""` 코드가 존재하지 않음

수정 전 계획 (dev_progress.md 487-503):
```csharp
// 토큰 삭제 코드가 제거됨
// if (!_autoLoginCheckBox.Checked)
// {
//     _config.Token = "";    // ← BUG 제거됨
// }
// 대신 autoLogin 플래그만 저장됨
```

현재 코드에 수정 적용됨:
- `_config.AutoLogin = _autoLoginCheckBox.Checked;` (라인 750)만 저장
- 현재 세션 토큰은 유지됨
- "로그인 정보 저장" 체크 해제 시만 이메일/서버주소 초기화 (라인 752-757)

### 수정 근거

**dev_progress.md 라인 386-510 (BUG-001 정의)**:
- 기획서에 "자동 로그인 미체크 시 토큰 삭제" 항목 없음 (라인 399)
- "자동 로그인"은 다음 시작 시 자동 로그인 여부만 결정 (라인 400-404)
- 현재 세션 토큰 유지 필요 (업로드에 필요)

---

## 빌드 결과

**명령어**: `dotnet publish -c Release -r win-x64 --self-contained`

**결과**: ❌ FAILURE

**경로**: N/A

**에러**:
```
command not found: dotnet
```

**원인**: macOS 환경에서 dotnet이 설치되지 않음
- 현재 실행 환경: macOS (Darwin 25.4.0)
- 필수 환경: Windows 또는 dotnet cross-compile 설정 필요
- **해결 방법**:
  - Windows 환경에서 직접 빌드, 또는
  - macOS에서 `brew install dotnet` 설치 후 재시도 필요

**빌드 가능 여부**: ⚠️ 환경 제약으로 리더는 빌드 불가
- 빌드 권한: 에이전트(Agent)가 수행 가능
- 에이전트에 위임 필요: "dotnet 설치 + Release 빌드 + exe 경로 확인"

---

## 코드 리뷰 기반 테스트 검증

### 테스트1: 날짜별 저장/중복 감지

- **상태**: ✅ 존재
- **파일**: OrderStorageService.cs
- **라인**: 44-136

**코드**:
```csharp
/// <summary>
/// 날짜별 파일 경로
/// </summary>
private static string GetFilePath(DateTime date)
{
    return Path.Combine(OrdersDir, $"orders_{date:yyyy-MM-dd}.json");
}

/// <summary>
/// 주문 저장
/// </summary>
public void Save(OrderRecord order)
{
    if (!Directory.Exists(OrdersDir))
        Directory.CreateDirectory(OrdersDir);

    var filePath = GetFilePath(DateTime.Now);
    var orders = LoadFromFile(filePath);

    // 순번 설정 (당일 기준)
    order.Seq = orders.Count > 0 ? orders.Max(o => o.Seq) + 1 : 1;

    orders.Add(order);

    // 임시 파일에 쓰고 교체 (손상 방지)
    var tmpPath = filePath + ".tmp";
    var options = new JsonSerializerOptions { WriteIndented = true };
    var json = JsonSerializer.Serialize(orders, options);
    File.WriteAllText(tmpPath, json);

    // 원본 파일로 교체
    if (File.Exists(filePath))
        File.Replace(tmpPath, filePath, null);
    else
        File.Move(tmpPath, filePath);
}

/// <summary>
/// 중복 감지 (최근 7일 해시 비교)
/// </summary>
public bool IsDuplicate(string hash)
{
    for (int i = 0; i < 7; i++)
    {
        var date = DateTime.Now.AddDays(-i);
        var filePath = GetFilePath(date);
        var orders = LoadFromFile(filePath);

        if (orders.Any(o => o.Hash == hash))
            return true;
    }
    return false;
}
```

**결론**: YES
- 날짜별 저장: `GetFilePath(DateTime.Now)` 사용하여 일자별 파일 생성 (라인 57, 108)
- 중복 감지: `IsDuplicate()` 메서드로 최근 7일 해시 비교 (라인 124-136)
- 데이터 무결성: 임시 파일 작성 후 교체로 손상 방지 (라인 66-75)

---

### 테스트2: 재전송 버튼 동작

- **상태**: ✅ 존재
- **파일**: MainForm.cs
- **라인**: 1034-1093

**코드**:
```csharp
private async void RetryUploadBtn_Click(object? sender, EventArgs e)
{
    var failedOrders = _orderStorage.GetFailedOrders();
    if (failedOrders.Count == 0)
    {
        MessageBox.Show("재전송할 주문이 없습니다.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
        return;
    }

    _retryUploadBtn.Enabled = false;
    _uploadStatusLabel.Text = $"재전송 중... ({failedOrders.Count}건)";
    _uploadStatusLabel.ForeColor = Color.Gray;

    int successCount = 0;
    int failCount = 0;
    string lastError = "";

    foreach (var order in failedOrders)
    {
        try
        {
            var serverUrl = _config.ServerUrl;
            if (string.IsNullOrEmpty(serverUrl))
                serverUrl = "https://agent.zigso.kr";

            try
            {
                await _uploadService.UploadReceiptAsync(_config.Token, serverUrl, _config.SiteId, order.ReceivedAt, order.Port, order.Content, order.Hash);
            }
            catch (UploadException ue) when (ue.StatusCode == 401)
            {
                // 토큰 만료 시 자동 재로그인 시도
                if (!string.IsNullOrEmpty(_config.Email) && !string.IsNullOrEmpty(_config.Password))
                {
                    await _authService.LoginAsync(_config.Email, _config.Password, serverUrl);
                    _config = LoginConfig.Load();
                    await _uploadService.UploadReceiptAsync(_config.Token, serverUrl, _config.SiteId, order.ReceivedAt, order.Port, order.Content, order.Hash);
                }
                else
                {
                    throw;
                }
            }

            _orderStorage.UpdateStatus(order.Hash, "성공");
            successCount++;
        }
        catch (Exception ex)
        {
            failCount++;
            lastError = ex.Message;
        }
    }

    _uploadStatusLabel.Text = failCount > 0
        ? $"재전송 완료: 성공 {successCount}건, 실패 {failCount}건 (마지막 에러: {lastError})"
        : $"재전송 완료: 성공 {successCount}건, 실패 {failCount}건";
    _uploadStatusLabel.ForeColor = failCount > 0 ? Color.Orange : Color.Green;
    _retryUploadBtn.Enabled = true;
}
```

**근거**:
- "실패" 상태 주문 조회: `_orderStorage.GetFailedOrders()` (라인 1036)
- 서버 재업로드: `_uploadService.UploadReceiptAsync()` (라인 1061)
- 실패 건 상태 업데이트: `_orderStorage.UpdateStatus(order.Hash, "성공")` (라인 1078)
- 토큰 만료 시 자동 재로그인: 401 에러 시 `LoginAsync()` 재시도 (라인 1063-1070)

**결론**: YES

---

### 테스트3: 자동 재로그인

- **상태**: ✅ 존재
- **파일**: MainForm.cs + AuthService.cs
- **라인**:
  - MainForm.cs: 698-715 (`TryAutoLogin()`)
  - AuthService.cs: 82-90 (`AutoLogin()`)

**코드 (MainForm.cs, 라인 698-715)**:
```csharp
private async void TryAutoLogin()
{
    try
    {
        var config = _authService.AutoLogin();
        _config = config;
        _autoLoginSuccess = true;

        // 바로 MainPanel 표시
        ShowMainPanel();
```

**코드 (AuthService.cs, 라인 82-90)**:
```csharp
/// <summary>
/// 자동 로그인 (저장된 토큰 확인)
/// </summary>
public LoginConfig AutoLogin()
{
    var config = LoginConfig.Load();
    if (!config.AutoLogin || string.IsNullOrEmpty(config.Token))
    {
        throw new Exception("저장된 토큰 없음");
    }
    return config;
}
```

**근거**:
- 앱 시작 시 자동 로그인 시도: MainForm 생성자에서 `TryAutoLogin()` 호출 (MainForm.cs 라인 72)
- 조건 검사: `config.AutoLogin` 플래그 확인 (AuthService.cs 라인 85)
- 저장된 토큰 유효성 확인: `config.Token` 존재 여부 검사 (AuthService.cs 라인 85)

**결론**: YES

---

### 테스트4: 재부팅 자동 시작

- **상태**: ✅ 존재
- **파일**: Program.cs
- **라인**: 16-26

**코드**:
```csharp
// Windows 자동 시작 등록
try
{
    var config = Models.LoginConfig.Load();
    using var key = Registry.CurrentUser.OpenSubKey(@"Software\Microsoft\Windows\CurrentVersion\Run", true);
    if (config.AutoStart)
        key?.SetValue("DeliveryOrderReceiver", Application.ExecutablePath);
    else
        key?.DeleteValue("DeliveryOrderReceiver", false);
}
catch { }
```

**근거**:
- Windows 레지스트리 접근: `Registry.CurrentUser.OpenSubKey()` (라인 20)
- 자동 시작 등록: `config.AutoStart` 플래그에 따라 레지스트리 설정 (라인 21-24)
- 등록 경로: `Software\Microsoft\Windows\CurrentVersion\Run` (라인 20)
- 앱 실행 경로: `Application.ExecutablePath` (라인 22)

**결론**: YES

---

## 최종 체크리스트

| 항목 | 상태 | 근거 |
|------|------|------|
| BUG-001 수정 | ✅ | MainForm.cs 라인 726-780: `_config.Token = ""` 제거됨 |
| 빌드 성공 | ❌ | macOS 환경, dotnet 미설치 (에이전트 위임 필요) |
| 테스트1 존재 | ✅ | OrderStorageService.cs 라인 44-136 (날짜별 저장/중복 감지) |
| 테스트2 존재 | ✅ | MainForm.cs 라인 1034-1093 (재전송 버튼) |
| 테스트3 존재 | ✅ | MainForm.cs 698-715 + AuthService.cs 82-90 (자동 재로그인) |
| 테스트4 존재 | ✅ | Program.cs 라인 16-26 (재부팅 자동 시작) |

---

## 결론

### v2.0.4 검증 상태

**✅ 코드 검증 완료**:
- BUG-001 수정: 완료 (토큰 삭제 코드 제거)
- 기획서 4대 기능: 모두 구현됨
  1. 날짜별 저장/중복 감지: ✅
  2. 재전송 버튼: ✅
  3. 자동 재로그인: ✅
  4. 재부팅 자동 시작: ✅

**❌ 빌드 미완료**:
- 원인: macOS 환경에서 dotnet 미설치
- 조치: 에이전트에 다음 작업 위임 필요

### 다음 단계

**에이전트 작업 (위임)**:
1. dotnet 설치 (brew install dotnet, 또는 Windows 환경에서)
2. Release 빌드 실행: `dotnet publish -c Release -r win-x64 --self-contained`
3. 빌드 결과 경로 확인 및 exe 파일 배포 준비

**PM 테스트 (현황 보류)**:
- BUG-001 수정 후 실제 업로드 동작 테스트
- 재전송 버튼 동작 확인
- 자동 재로그인 동작 확인
- 재부팅 자동 시작 테스트

---

**검증 일시**: 2026-04-04
**검증 환경**: macOS (Darwin 25.4.0)
**검증자**: 리더 (코드 검사)
